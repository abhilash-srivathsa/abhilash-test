# abhilash-test
Testing + local setup

## Feature flag rollout

This branch adds a complete feature flag rollout workflow in one commit. It
supports flag creation, rollout percentages, explicit allow/block user lists,
status-based evaluation, stable user bucketing, API client helpers, and focused
tests for rollout behavior and validation.
